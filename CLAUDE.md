# J-Onboard

日本に3ヶ月を超えて滞在する交換留学生・客員研究者向けに、ビザ書類の受け取りから来日後の生活準備までの手続きを案内するWebアプリ。利用者の大半は日本語が読めず、スマホで使う。デプロイ先: https://j-onboard.vercel.app/

## コマンド

- `npm start` — 開発サーバー（Vite、http://localhost:3000）
- `npm test` — テストを1回だけ実行（Vitest）
- `npm run build` — 本番ビルド（`dist/` に出力。Vercel の設定は `vercel.json`）

## 構成

ほぼすべて `src/App.jsx` にある。

- `T` — UI文言（`en` / `ja`）。フォームや共通ラベルはここで2言語を持つ
- `buildPhases(profile, lang)` — ロードマップのタスクデータ。**タスク本文は英語のみ**（日本語はタイトルと窓口フレーズだけ）
- `TaskCard` — タスク1件の表示。見出し全体のタップで開閉、チェック丸は `stopPropagation`
- `JOnboard` — 画面全体。進捗・回答は localStorage（`jonboard_checked` / `jonboard_profile` / `jonboard_step`）に保存

### タスクの項目

| 項目 | 意味 |
|---|---|
| `level` | `required`（法的に必須、または飛ばすと先に進めない）/ `recommended`（任意だがやらないと損）/ `optional` |
| `deadline.days` | 住み始めた日からの法定期限。`required` のタスクにだけ付ける。バッジに「within N days of moving in」と出る。起点が住み始めた日でない期限（国保は市によって来日日から数える）には付けず、本文に書く |
| `deps` | `REQUIRED` は「Complete this first」表示と「次にやること」の判定に使う。`STRONGLY_ADVISED` は現在表示していない |
| `counter` / `counterTranslation` | 窓口で見せる日本語フレーズとその英訳 |
| `source` | 公式情報のURLと確認した年月（`verified: "YYYY-MM"`）。民間の手続きは `null` |

## 内容のルール

- **公式情報で確認できた事実だけを載せる。** 行政手続きのアプリは、誤った情報を1つ出した時点で使ってもらえなくなる。手続きの条件・金額・期限・対象者を書くときは、nenkin.go.jp・mhlw.go.jp・isa.go.jp・自治体などの公式ページで確認し、`source` と `verified` を更新する。確認できないことは載せず、人によって違う場合は「窓口で確認する」形にする
- **英語UIでは、日本語の正式名称はタスクのタイトルに1回だけ**「英語名 (日本語名)」の形で出す。場所・持ち物・説明文・バッジは英語のみ
- 見出しのバッジは短いラベルだけにする。説明は開いた中の本文に書く

## 設計の方針

- 答えによって結果が変わらない質問は置かない（滞在期間の設問はこの理由で削除した）
- スマホ幅（390px程度）で表示を確認する。冒頭の文章は最初の質問が1画面目に入る量に抑える
- 利用者の移動（役所に行く回数）と、来日直後の人には難しい作業（書類の郵送など）を増やさない案内にする

## 作業の進め方

- **コードの書き換え・コミット・プッシュは、ユーザーの了承を得てから行う。** 提案や相談への回答の流れで勝手に始めない。「進めて」と言われたのがコードの変更だけなら、コミットとプッシュはそれぞれ改めて確認する
- コミットは `main` に直接行う。コミットメッセージは英語で、変更の理由を本文に書く
- **コミットの前に、ユーザーがローカルで表示を確認する。** 変更後は `npm test` を実行し、開発サーバーを起動して http://localhost:3000 のリンクと、見てほしい画面・タスクを伝える。「確認した」と言われてからコミットを提案する
